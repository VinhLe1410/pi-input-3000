{
  "name": "pi-custom-input",
  "private": true,
  "description": "Amp-inspired and sticky input UI for Pi Coding Agent",
  "scripts": {
    "lint": "eslint .",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.80.10",
    "@earendil-works/pi-tui": "0.80.10",
    "@types/node": "24.13.1",
    "eslint": "^10.5.0",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.62.0"
  }
}
